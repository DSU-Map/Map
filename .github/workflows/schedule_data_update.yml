name: 🗓️ Scheduled Data Update (Menu OCR)

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  run_python_script:
    # ✅ 기본 작업 디렉토리를 DSUMAP_ROOT로 설정
    defaults:
      run:
        working-directory: DSUMAP_ROOT 
        
    runs-on: ubuntu-latest

    steps:
    - name: 1. 코드 체크아웃
      uses: actions/checkout@v4
      with:
        # ✅ path: 'DSUMAP_ROOT'는 제거하고, 토큰만 남김
        token: ${{ secrets.GITHUB_TOKEN }} 
        
    - name: 2. 파이썬 환경 설정
      uses: actions/setup-python@v5
      with:
        python-version: '3.11' 
        
    - name: 3. 필수 라이브러리 설치
      run: |
        pip install google-genai pillow
        
    - name: 4. 파이썬 스크립트 실행 및 JSON 생성
      run: |
        # ✅ 작업 디렉토리가 DSUMAP_ROOT이므로 바로 backend/ 실행
        python backend/ocr_importer_local.py
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }} 
        
    - name: 5. 변경된 JSON 파일 커밋 및 푸시
      run: |
        # ✅ CWD가 DSUMAP_ROOT이므로 Map/Public/으로 바로 접근
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add Map/Public/menu_data.json
        git commit -m "chore(data): 자동 스케줄링된 식단 데이터 업데이트 [skip ci]" || echo "변경 사항 없음"
        git push
